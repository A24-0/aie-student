# Самопроверка проекта (self-checklist)

| #  | Критерий                                                                 | Да/Нет | Где смотреть / комментарий                          |
|----|---------------------------------------------------------------------------|--------|-----------------------------------------------------|
| 1  | Сервис запускается по инструкциям из `README.md` и работает              | ✅     | `python -m src.service.main`, раздел 4 README       |
| 2  | Endpoint `/predict` использует **реальную модель**, а не заглушку        | ✅     | гибрид BM25+эмбеддинги, `src/rag/engine.py`         |
| 3  | Есть EDA и хотя бы один эксперимент с метриками                          | ✅     | `notebooks/01_eda.ipynb`, `02_retrieval_experiments.ipynb` |
| 4  | Есть baseline и улучшенная модель, есть **сравнение по метрикам**        | ✅     | BM25/dense → hybrid, `artifacts/retrieval_comparison.csv` |
| 5  | Код не свален в один ноутбук: внятная структура в `src/`                 | ✅     | `src/data`, `src/retrieval`, `src/rag`, `src/eval`, `src/service` |
| 6  | Есть Dockerfile **или** понятный сценарий развёртывания                  | ✅     | `Dockerfile` + шаги запуска в `README.md` §4        |
| 7  | Есть `.env.example` и **нет** реальных секретов в репозитории            | ✅     | `configs/.env.example`, `.env` в `.gitignore`       |
| 8  | Реализованы логи/наблюдаемость (логи + `/health`)                        | ✅     | `logging`, `/health`, `/metrics`, middleware задержки |
| 9  | В `report.md` **обоснован выбор финальной модели** по экспериментам      | ✅     | `report.md` §5                                      |
| 10 | README и report позволяют понять сценарий демонстрации                   | ✅     | README §7, report §9                                |

**Самооценка: 10 / 10.** Финальные метрики (hybrid, top_k=5): hit@5 = 0.92,
recall@5 = 0.91, MRR@5 = 0.81, nDCG@5 = 0.83. Итоговое решение по оценке — за преподавателем.
